<%= link_to '新体验店', new_admin_shop_path %>
<h1>体验店列表</h1>

<table class="table table-hover table-condensed">
  <tr>
    <th>体验店</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @shops.each do |shop| %>
  <tr>
    <td><%= shop.name %></td>
    <td><%= shop.address %></td>
    <td><%= link_to '显示', [:admin,shop] %></td>
    <td><%= link_to '编辑', edit_admin_shop_path(shop) %></td>
    <td><%= link_to '删除', [:admin,shop], confirm: '确定?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />


